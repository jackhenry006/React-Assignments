# React-Assignments

Interview Question

What is memory leak in react

It occurs in useEffect as when we use an set interval and and we do not use the return statement as it is the unmounting phase the memory leak occurs .
